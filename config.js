export const gridWidth = 32;
export const gridSize = gridWidth * gridWidth;
export let lineColor = 'rgb(0, 0, 0)';
export let backgroundColor = 'rgb(255, 223, 223)';
export const updateTime = 250;
export const strokeSize = 1;

export const setLineColor = (color) => {
	lineColor = color;
};

export const setBackgroundColor = (color) => {
	backgroundColor = color;
};
